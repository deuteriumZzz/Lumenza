# Ограничивает длину сохраняемого текста ошибки от стороннего
# провайдера: держит поля error_message в разумных пределах и снижает
# риск того, что необычно многословное исключение SDK (например,
# повторяющее детали запроса) целиком попадёт в БД и будет показано в
# admin как есть.
ERROR_MESSAGE_MAX_LEN = 500
