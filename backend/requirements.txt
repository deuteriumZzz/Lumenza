Django==5.0.9
djangorestframework==3.15.2
psycopg2-binary==2.9.9
celery==5.4.0
redis==5.0.8
python-dotenv==1.0.1
django-environ==0.11.2
gunicorn==22.0.0
openai==1.109.1
# openai==1.51.0 was previously pinned together with httpx==0.27.2 to work
# around a `proxies` kwarg openai's own internal httpx.Client() call passed
# that httpx>=0.28 had removed. Bumped to 1.109.1 (fixed upstream) so httpx
# could move to >=0.28.1 instead — required by google-genai below.
httpx==0.28.1
anthropic==0.117.0
# Replaces google-generativeai (officially deprecated, "all support has
# ended" per its own FutureWarning) — forced by a hard, unresolvable pip
# conflict: nvidia-riva-client (below) pins protobuf==6.33.5 exactly, while
# google-ai-generativelanguage (google-generativeai's dependency) requires
# protobuf<6.0.0dev. google-genai has no protobuf dependency at all, so this
# is the only way to have both real Gemini calls and real Riva ASR/TTS in
# the same environment. See providers/gemini_adapter.py for the API change.
google-genai==2.12.1
Pillow==12.3.0
replicate==1.0.7
# Direct dependency for imagegen/nvidia_image_adapter.py — was only ever
# present transitively (via google-api-core) before.
requests==2.34.2
aiogram==3.30.0
pytest==8.3.3
pytest-django==4.9.0
# gRPC client for NVIDIA's cloud-hosted Riva ASR/TTS — confirmed
# live-working (unlike the REST-based 404s documented in SPEC.md):
# real transcription/synthesis calls succeed against grpc.nvcf.nvidia.com:443
# using a model-specific "function-id" gRPC metadata header alongside the
# usual Bearer token, found via web search of NVIDIA's Riva client examples
# (not in build.nvidia.com's own REST-oriented API docs). Its exact
# protobuf==6.33.5 pin is what forced the google-generativeai -> google-genai
# migration above.
nvidia-riva-client==2.26.0

# Serves static files (admin CSS/JS) under gunicorn — unlike runserver,
# gunicorn doesn't serve STATIC_URL itself.
whitenoise==6.7.0

# Live voice mode — WebSocket support (Django Channels) + the ASGI server
# that replaces gunicorn for the `web` service. No channel layer/Redis
# needed: each voice consumer only relays its own connection 1:1 to
# Gemini Live, it never broadcasts to other consumers.
channels==4.1.0
daphne==4.1.2
pytest-asyncio==0.24.0
